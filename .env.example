DATABASE_URL=postgresql://app_user:devpassword_app@localhost:5432/whatsapp_saas_dev
JWT_SECRET=cambiar-por-un-secreto-largo-y-aleatorio
# ENCRYPTION_KEY debe ser EXACTAMENTE 32 bytes en hexadecimal (64 caracteres).
# Generar con: openssl rand -hex 32
ENCRYPTION_KEY=
PORT=3001
WHATSAPP_VERIFY_TOKEN=elige-cualquier-texto-y-anotalo-en-meta-tambien
ANTHROPIC_API_KEY=
VOYAGE_API_KEY=
# DSN del proyecto en Sentry (docs.sentry.io/platforms/javascript/guides/nestjs/).
# Si se deja vacio, el SDK arranca en modo no-op: no manda nada, pero no
# rompe el arranque en local.
SENTRY_DSN=
